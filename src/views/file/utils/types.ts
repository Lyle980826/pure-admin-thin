export interface FormProps {
  formInline: {
    title: string;
    filename: string;
    fileType: string;
    fileSize: string;
    uploadTime: string;
    remark: string;
    fileList: any[];
  };
}
